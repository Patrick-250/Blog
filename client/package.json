{
  "name": "booking",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.1.1",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.7.9",
    "dompurify": "^2.4.0",
    "moment": "^2.29.4",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-quill": "^0.0.2",
    "react-router-dom": "^6.4.1",
    "react-scripts": "^3.0.1",
    "sass": "^1.83.4",
    "sass-loader": "^16.0.4",
    "web-vitals": "^2.1.4",
    "webpack": "^4.42.0"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && react-app-rewired start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-app-rewired build",
    "test": "set NODE_OPTIONS=--openssl-legacy-provider && react-app-rewired test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:8800",
  "devDependencies": {
    "react-app-rewired": "^2.2.1"
  }
}